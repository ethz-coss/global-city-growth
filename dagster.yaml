storage:
  postgres:
    postgres_db:
      hostname:
        env: POSTGRES_HOST
      port:
        env: POSTGRES_PORT
      db_name:
        env: POSTGRES_DB
      username:
        env: POSTGRES_USER
      password:
        env: POSTGRES_PASSWORD